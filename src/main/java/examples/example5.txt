class LightSwitch is
    var state: Boolean
    this(state: Boolean) is
        this.state := state
    end
    method toggle() is
        if this.state then
            this.state := false
        else
            this.state := true
        end
    end
end