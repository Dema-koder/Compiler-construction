class Shape is
    method area() Real is
        return 0
    end
end
class Circle is
    var radius: Real
    this(radius: Real) is
        this.radius := radius
    end
    method area() Real is
        return radius.Mult(radius).Mult(3)
    end
end